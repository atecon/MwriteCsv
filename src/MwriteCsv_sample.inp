set verbose off
clear

include MwriteCsv.gfn

scalar n_rows = 5
matrix m = mnormal(n_rows, 5)
cnameset(m, "date B C D E")
strings rnam = array(n_rows)
loop i=1..n_rows -q
    rnam[i] = sprintf("R%d", $i)
endloop
rnameset(m, rnam)
m[1,1] = NA

MwriteCsv(m, "foo.csv")

eval readfile("foo.csv")
open "foo.csv" -p -q

